# Demo Assets

`gum-demo.png` is included so the browser microscope has an immediate sample
image on page load.

This image is a demo asset with a separate license from the MIT code:

- allowed: viewing, testing, local demo use, screenshots of the demo output
- not allowed: reuse in games, animation, tools, asset packs, resale, or model
  extraction for another project

The repo code can remain open while the character asset stays protected.
